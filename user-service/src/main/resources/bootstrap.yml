spring:
  application:
    name: user-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
      config:
        import:
          - configserver:
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/