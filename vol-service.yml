# FICHIER 3 : vol-service.yml (Dans le Dépôt Git)

spring:
  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/XEPDB1
    username: travel_app_user
    password: MON_MOT_DE_PASSE_DB
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
